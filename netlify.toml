# Datum Details — Netlify configuration
# Static, no-build site. All site files live at the repo root, so we publish
# the root directly. No build command, no base directory needed.

[build]
  publish = "."
